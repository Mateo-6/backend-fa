/**
 * Re-export Transaction interface, TransactionType enum, and CategorySnapshot interface from fa-contracts package.
 * This file maintains backward compatibility with existing imports.
 */
export type { Transaction, CategorySnapshot } from 'fa-contracts';
export { TransactionType } from 'fa-contracts';

